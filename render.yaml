services:
  - type: web
    name: wacky-chess-variants
    runtime: node
    plan: free
    buildCommand: npm install --workspaces=false && npm --prefix client install && npm --prefix client run build
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
